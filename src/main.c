#include <stdio.h>
#include "../include/flight_math.h"
int main(void) {
    Lander lander = {
        .mass = 0,
        .velocity = 0,
        .drag_coefficient = 0,
        .altitude = 0,
        .area = 0,
        .acceleration = 0,
        .deployment_altitude = 0,
        .name= "sky-lander",
        .deployed = 0
    };
    while ()
    return 0;
}